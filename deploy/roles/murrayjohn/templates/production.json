{
	"port": 3000,
	"mongo": "mongodb://localhost/murrayjohn",
  "cloudinary config": {
    "cloud_name": "{{secrets.cloudinary.cloud_name}}",
    "api_key": "{{secrets.cloudinary.api_key}}",
    "api_secret": "{{secrets.cloudinary.api_secret}}"
  }
}
